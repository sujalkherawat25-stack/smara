services:
  api:
    volumes:
      - type: bind
        source: ${SMARA_VAPID_PRIVATE_KEY_HOST_FILE:-./secrets/vapid_private.pem}
        target: /run/secrets/smara_vapid_private.pem
        read_only: true
